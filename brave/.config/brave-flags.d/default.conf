# Session/host-agnostic defaults. Applied by the ~/.local/bin/brave shim for
# every host, before the host- or session-specific file below.
--ignore-gpu-blocklist
--enable-gpu-rasterization
--enable-oop-rasterization
--enable-zero-copy
