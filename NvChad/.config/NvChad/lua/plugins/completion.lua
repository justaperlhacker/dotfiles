return {
  {
    "hrsh7th/nvim-cmp",
    opts = function(_, opts)
      -- Example: Modify sources or behavior
      -- opts.sources = vim.list_extend(opts.sources, { { name = "path" } })
      return opts
    end,
  },
}
