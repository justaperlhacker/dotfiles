--password-store=kwallet
# GPU flags for AMD Radeon RX 6700 XT. Chromium honours only ONE --enable-features
# value, so all features must be comma-separated on a single line.
--ignore-gpu-blocklist
--enable-zero-copy
--enable-features=VaapiVideoDecoder,CanvasOopRasterization
