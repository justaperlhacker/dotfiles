# AMD Radeon RX 6700 XT (destro). Chromium honours only ONE --enable-features
# value, so all features must be comma-separated on a single line.
--ozone-platform-hint=auto
--enable-features=VaapiVideoDecoder,CanvasOopRasterization
